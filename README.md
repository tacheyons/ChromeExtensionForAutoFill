# ChromeExtensionForAutoFill
ChromeExtensionForAutoFill WorkDay Pattern forms

* Add the chrome extension 
* Allows to fill the workday job application fill once and use multiple times.
* You can add as much as fields by updating the field id object FIELD_MAP.
* Customize as the field grows.
